asset_channel_configurations:
    mobile:
        configuration:
            scale: { width: 200 }
            colorspace: { colorspace: gray }
        code: mobile
    print:
        configuration:
            resize: { width: 400, height: 500 }
        code: print
    ecommerce:
        configuration:
            scale: { ratio: 50 }
        code: ecommerce
